import { Router } from 'express';

const router = Router();

// This file is reserved for additional ComfyUI proxy routes
// The main proxy logic is in api.ts (/view, /upload endpoints)

export default router;
